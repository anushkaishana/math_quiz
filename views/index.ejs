<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= quizName %></title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
</head>
<body class="homepage">
    <header class="homepage-header">
        <h1>Welcome to <%= quizName %>!</h1>
        <br>
        <p>
            Our app helps you test your knowledge and grow through interactive quizzes. 
            Join us today to explore and improve in a fun and engaging way!
        </p>
        <br>
        <a href="<%= baseURL %>/about" class="about-link">Find out more about us...</a>
    </header>
    
    <main class="homepage-main">
        <section class="intro">
            <p>Get ready to test your knowledge with our quiz!</p>
            <h2>Remember to do your best, and don't worry, you're only going to get better</h2>
        </section>

        <section class="actions">
            <a href="<%= baseURL %>/register">
                <button>Create new account</button>
            </a>

            <% if (!isLoggedIn) { %>
                <a href="<%= baseURL %>/login"><button>Sign in & Get Started</button></a>
            <% } else { %>
                <button disabled>Sign in & Get Started</button>
            <% } %>
        </section>

        <% if (isLoggedIn) { %>
            <section class="quiz-link">
                <a href="<%= baseURL %>/quiz-list">Head back to Quizzing</a>
            </section>
        <% } %>
    </main>
</body>
</html>


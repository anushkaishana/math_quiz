<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
    <title>Choose a Quiz</title>
</head>
<body class="quiz-list-page">
    <div class="quiz-list-header">
        <a href="<%= baseURL %>/" >
            <img src="home-icon.png" alt="Head back home">
        </a>  
        <a href="<%= baseURL %>/profile" >
            <img src="user-icon.png" alt="View your prodile!">
        </a>  
    </div>
    
    <h1>Choose a Quiz</h1>
    <ul class="quiz-list">
        <% if (progress.level >= 1) { %>
            <li><a href="/quiz?quizId=1">Addition Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Addition Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 2) { %>
            <li><a href="/quiz?quizId=2">Subtraction Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Subtraction Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 3) { %>
            <li><a href="/quiz?quizId=3">Multiplication Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Multiplication Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 4) { %>
            <li><a href="/quiz?quizId=4">Division Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Division Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 5) { %>
            <li><a href="/quiz?quizId=5">Geometry Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Geometry Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 6) { %>
            <li><a href="/quiz?quizId=6">Time Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Time Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 7) { %>
            <li><a href="/quiz?quizId=7">Algebra Level 1 Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Algebra Level 1 Quiz (Locked)</li>
        <% } %>

        <% if (progress.level >= 8) { %>
            <li><a href="/quiz?quizId=8">Algebra Level 2 Quiz</a></li>
        <% } else { %>
            <li class="locked">🔒 Algebra Level 2 Quiz (Locked)</li>
        <% } %>
    </ul>
</body>
</html>


<!-- lock emoji: https://gist.github.com/rxaviers/7360908 -->
<!-- home emoji: https://icons8.com/icons/set/home-gray-->
<!-- <a target="_blank" href="https://icons8.com/icon/15263/male-user">Male User</a> icon by <a target="_blank" href="https://icons8.com">Icons8</a>-->